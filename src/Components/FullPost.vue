<template>
    <div>
        <h3>TEste full post</h3>
        <!-- <p>geting the id from the router:  {{ $route.params.id }}</p>

        <p>{{ $data }}</p>
      <div v-for="(posts, index) in post" v-bind:key="index"> 
            <h2>{{posts.title}}</h2>
            <p>{{posts.content}}</p>
            <p>{{posts.lastPosted}} - {{posts.author}}</p> 
            </div> 
            
            <button @click="showPost">Show the post</button> -->
        
    </div>
</template>


<script>

export default {

   
}
</script>
